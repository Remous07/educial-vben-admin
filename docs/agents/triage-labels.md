# Triage Labels

The `triage` skill uses the following label mapping:

| Role | Label |
|------|-------|
| needs-triage | `needs-triage` |
| needs-info | `needs-info` |
| ready-for-agent | `ready-for-agent` |
| ready-for-human | `ready-for-human` |
| wontfix | `wontfix` |
