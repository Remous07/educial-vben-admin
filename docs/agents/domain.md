# Domain Docs

This repo uses a **single-context** layout.

- **CONTEXT.md** — domain language glossary (at repo root)
- **docs/adr/** — architecture decision records

Skills that read domain docs (`improve-codebase-architecture`, `diagnose`, `tdd`) will look for these paths.
